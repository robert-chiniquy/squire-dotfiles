[user]
	email = robert.chiniquy@conductorone.com
	name = rch
[push]
	autoSetupRemote = true
[git]
	difftool = vimdiff
[core]
	pager = delta
	excludesfile = ~/.gitignore_global
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
	light = false
	side-by-side = true
	line-numbers = true
	wrap-max-lines = unlimited
	# Vaporwave colors with readable contrast
	commit-decoration-style = bold "#fbb725" box ul
	file-style = bold "#ff00f8"
	file-added-label = [+]
	file-modified-label = [M]
	file-removed-label = [-]
	file-renamed-label = [R]
	hunk-header-style = file line-number syntax bold "#5cecff"
	hunk-header-decoration-style = "#aa00e8" box
	# Removed lines: hot pink text on dark red background
	minus-style = syntax "#330011"
	minus-non-emph-style = syntax "#330011"
	minus-emph-style = syntax "#550022"
	minus-empty-line-marker-style = normal "#330011"
	# Added lines: cyan text on dark green background
	plus-style = syntax "#002222"
	plus-non-emph-style = syntax "#002222"
	plus-emph-style = syntax "#004444"
	plus-empty-line-marker-style = normal "#002222"
	# Line numbers in vaporwave colors
	line-numbers-minus-style = "#ff0099"
	line-numbers-plus-style = "#5cecff"
	line-numbers-zero-style = "#666666"
	line-numbers-left-format = "{nm:>4}┊"
	line-numbers-right-format = "{np:>4}│"
	line-numbers-left-style = "#ff00f8"
	line-numbers-right-style = "#5cecff"
	zero-style = syntax
	whitespace-error-style = "#fbb725" reverse
[merge]
	conflictstyle = diff3
[diff]
	colorMoved = default
	algorithm = histogram
	# Syntax-aware diffs via difftastic (comment out to use delta instead)
	# external = difft
	# Word-level diff for prose/comments
	wordRegex = [a-zA-Z0-9_]+|[^[:space:]]
[fetch]
	parallel = 0
	prune = true
[gc]
	auto = 256
[rerere]
	enabled = true
[rebase]
	autoStash = true
[pull]
	rebase = true
[color]
	ui = auto
[color "status"]
	added = "#5cecff" bold
	changed = "#ff0099"
	untracked = "#fbb725"
	branch = "#5cecff" bold
	header = "#aa00e8"
[color "branch"]
	current = "#5cecff" bold
	local = "#ff00f8"
	remote = "#fbb725"
	upstream = "#aa00e8"
[color "diff"]
	meta = "#ff00f8" bold
	frag = "#aa00e8" bold
	old = "#ff0099"
	new = "#5cecff"
	commit = "#fbb725" bold
[alias]
	s = status -sb
	co = checkout
	br = branch
	ci = commit
	unstage = reset HEAD --
	last = log -1 HEAD --stat
	lg = log --oneline --graph --decorate -20
	branches = branch -a --sort=-committerdate
	stashes = stash list
	remotes = remote -v
	amend = commit --amend --no-edit
	undo = reset --soft HEAD~1
	wip = commit -am 'WIP'
	# Diff variations
	d = diff
	ds = diff --staged
	dw = diff --ignore-all-space
	dws = diff --staged --ignore-all-space
	dww = diff --word-diff --ignore-all-space
	# Syntax-aware diff via difftastic
	dft = "!f() { GIT_EXTERNAL_DIFF=difft git diff \"$@\"; }; f"
	dfts = "!f() { GIT_EXTERNAL_DIFF=difft git diff --staged \"$@\"; }; f"
	# Inline diff (no side-by-side, good for narrow terminals)
	di = -c delta.side-by-side=false diff
	dis = -c delta.side-by-side=false diff --staged
